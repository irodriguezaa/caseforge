[pytest]
; This project's domain models are literally named TestCase/TestStep/TestCaseCreate, etc.,
; which otherwise triggers pytest's default class-based collection heuristics. All tests in
; this suite are plain functions, so class-based collection is disabled entirely.
python_classes =
